<% @plans.each do |plan|%>
  <div class="col-lg-3 col-md-4 col-md-6 col-sm-12">
    <div class="card-trip-plan">
      <%= cl_image_tag plan.photo %>
      <div class="card-trip-infos">
        <div>
          <h4 class="tab-text-content pl-2"><%= link_to plan.name, plan_path(plan), { :class=>"stretched-link" }  %></h4>
          <!-- <p><%= plan.description.truncate_words(10)%></p> -->
        </div>
        <%= link_to (cl_image_tag plan.kitchen.user.photo, class:"card-trip-user-big avatar-bordered"), kitchen_path(plan.kitchen) %>
      </div>
      <div class="mx-3 pl-3">
        <div class="mealcard-height">
          <div class='row'>
            <% plan.tag_list.each do |tag| %>
              <%= link_to '#'+tag, plans_path(tag: tag), class: "tab-text-tag mx-1 px-1" %>
            <% end %>
          </div>
        </div>
        <div class='row d-flex align-items-end flex-column bd-highlight mb-3'>
          <h5 class='pr-3'><%=humanized_money_with_symbol(plan.price)%>/day</h5>
        </div>
      </div>
    </div>
  </div>
<% end %>