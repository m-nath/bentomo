<title>Bentomo Meal Plans</title>
<h1>Plans</h1>
<div class="container">
  <% Plan.all.each do |plan|%>
    <ul>
      <li><%= plan.name %></li>
      <li><%= plan.price %></li>
      <li><%= link_to "Details", plan_path(plan) %></li>
    </ul>
  <% end %>
</div>